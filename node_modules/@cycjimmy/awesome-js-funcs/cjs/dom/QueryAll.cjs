"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;
var _isString = _interopRequireDefault(require("../judgeBasic/isString.cjs"));
var _nodeListToArray = _interopRequireDefault(require("../typeConversion/nodeListToArray.cjs"));
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
var QueryAll = /*#__PURE__*/function () {
  /**
   * QueryAll
   * @param selectorOrEls
   * @param context
   */
  function QueryAll(selectorOrEls) {
    _classCallCheck(this, QueryAll);
    var context = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : document;
    var elements;
    if ((0, _isString["default"])(selectorOrEls)) {
      elements = context.querySelectorAll(selectorOrEls);
    } else {
      elements = selectorOrEls;
    }
    this.nodeList = (0, _nodeListToArray["default"])(elements);
  }

  /**
   * event bind
   * @param eventType
   * @param fn
   * @returns {QueryAll}
   */
  _createClass(QueryAll, [{
    key: "on",
    value: function on(eventType, fn) {
      var aEvents = eventType.split(' ');
      this.nodeList.forEach(function (el) {
        aEvents.forEach(function (event) {
          el.addEventListener(event, fn);
        });
      });
      return this;
    }

    /**
     * addClass
     * @param className
     * @returns {QueryAll}
     */
  }, {
    key: "addClass",
    value: function addClass(className) {
      this.nodeList.forEach(function (el) {
        if (el.classList) {
          el.classList.add(className);
        } else {
          // eslint-disable-next-line no-param-reassign
          el.className += " ".concat(className);
        }
      });
      return this;
    }

    /**
     * removeClass
     * @param className
     * @returns {QueryAll}
     */
  }, {
    key: "removeClass",
    value: function removeClass(className) {
      this.nodeList.forEach(function (el) {
        if (el.classList) {
          el.classList.remove(className);
        } else {
          // eslint-disable-next-line no-param-reassign
          el.className = el.className.replace(new RegExp("(^|\\b)".concat(className.split(' ').join('|'), "(\\b|$)"), 'gi'), ' ');
        }
      });
      return this;
    }

    /**
     * hasClass
     * @param className
     * @returns {QueryAll}
     */
  }, {
    key: "hasClass",
    value: function hasClass(className) {
      this.nodeList.forEach(function (el) {
        if (el.classList) {
          el.classList.contains(className);
        } else {
          new RegExp("(^| )".concat(className, "( |$)"), 'gi').test(el.className);
        }
      });
      return this;
    }

    /**
     * remove element
     */
  }, {
    key: "remove",
    value: function remove() {
      this.nodeList.forEach(function (el) {
        el.parentNode.removeChild(el);
      });
    }
  }]);
  return QueryAll;
}();
exports["default"] = QueryAll;