"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;
var _QueryAll = _interopRequireDefault(require("./QueryAll.cjs"));
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }
/* eslint func-names: ["error", "as-needed"] */
/**
 * touchActive
 * @param selectorOrEls
 * @param context
 * @param isMoveShowActive
 * @param activeClassName
 */
var _default = function _default(selectorOrEls, _ref) {
  var _ref$context = _ref.context,
    context = _ref$context === void 0 ? document : _ref$context,
    _ref$isMoveShowActive = _ref.isMoveShowActive,
    isMoveShowActive = _ref$isMoveShowActive === void 0 ? false : _ref$isMoveShowActive,
    _ref$activeClassName = _ref.activeClassName,
    activeClassName = _ref$activeClassName === void 0 ? 'touch-active' : _ref$activeClassName;
  var element = new _QueryAll["default"](selectorOrEls, context);

  /**
   * addTouchActive
   */
  var addTouchActive = function addTouchActive() {
    if (!this.classList.contains(activeClassName)) {
      this.classList.add(activeClassName);
    }
  };

  /**
   * removeTouchActive
   */
  var removeTouchActive = function removeTouchActive() {
    if (this.classList.contains(activeClassName)) {
      this.classList.remove(activeClassName);
    }
  };
  element.on('touchstart', addTouchActive);

  // If you want to display touchActive while moving, do not execute removeTouchActive
  if (!isMoveShowActive) {
    element.on('touchmove', removeTouchActive);
  }
  element.on('touchend', removeTouchActive);
  element.on('touchcancel', removeTouchActive);
};
exports["default"] = _default;