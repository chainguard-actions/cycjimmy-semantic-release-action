/**
 * singleton constructor(design patterns)
 * @returns {function(*=)}
 * @constructor
 */
export default (() => {
  var instance;
  return newInstance => {
    if (newInstance) {
      instance = newInstance;
    }

    return instance;
  };
});