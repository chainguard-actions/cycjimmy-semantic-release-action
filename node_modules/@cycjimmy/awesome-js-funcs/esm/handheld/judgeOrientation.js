/**
 * judge orientation
 * @return {string}
 */
export default (() => {
  var {
    clientWidth,
    clientHeight
  } = document.documentElement;
  return clientWidth > clientHeight ? 'landscape' : 'portrait';
});