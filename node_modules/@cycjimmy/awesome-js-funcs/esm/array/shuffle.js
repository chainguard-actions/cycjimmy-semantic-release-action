import randomInt from '../math/randomInt';
/**
 * shuffle
 * @param array
 * @returns {*[]}
 */

export default (function () {
  var array = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var m = array.length;

  while (m) {
    m -= 1;
    var random = randomInt(0, m); // change

    [array[m], array[random]] = [array[random], array[m]];
  }

  return array;
});