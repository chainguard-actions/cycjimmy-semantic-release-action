/* eslint func-names: ["error", "as-needed"] */
import QueryAll from './QueryAll';
/**
 * touchActiveMockClick
 * @param selectorOrEls
 * @param context
 * @param activeClassName
 */

export default ((selectorOrEls, _ref) => {
  var {
    context = document,
    activeClassName = 'touch-active'
  } = _ref;
  var element = new QueryAll(selectorOrEls, context);
  /**
   * addTouchActive
   */

  var addTouchActive = el => {
    if (!el.classList.contains(activeClassName)) {
      el.classList.add(activeClassName);
    }
  };
  /**
   * removeTouchActive
   */


  var removeTouchActive = el => {
    if (el.classList.contains(activeClassName)) {
      el.classList.remove(activeClassName);
    }
  };

  element.on('click', () => {
    addTouchActive(element);
    setTimeout(() => {
      removeTouchActive(element);
    }, 200);
  });
});