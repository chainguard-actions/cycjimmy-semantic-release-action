/**
 * get url relative path root directory
 * @returns {string|*}
 */
export default (() => {
  var url = document.location.toString();
  var arrUrl = url.split('//');
  var start = arrUrl[1].indexOf('/');
  var final = arrUrl[1].lastIndexOf('/');
  return arrUrl[1].substring(start, final);
});